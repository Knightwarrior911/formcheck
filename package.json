{
  "name": "formcheck",
  "version": "0.1.0",
  "description": "AI fitness form coach — real-time pose tracking, rep counting, form feedback. 100% local.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
